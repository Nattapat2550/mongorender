<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>Home</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h1>สวัสดี <%= user.name %></h1>
    <p>Role: <%= user.role %></p>
    <% if (user.role === "admin") { %>
      <p>คุณคือผู้ดูแลระบบ</p>
    <% } else { %>
      <p>คุณคือผู้ใช้ทั่วไป</p>
    <% } %>
    <a href="/">ออกจากระบบ</a>
  </div>
</body>
</html>
